#
#
# ISR Required Package

$ sudo apt-get install libfuse-dev libcurl3 zlib1g-dev liblzma-dev e2fsprogs libarchive-dev libgtk2.0-dev ssh openssh-client python-gtk2-dev python-gtk-vnc python-yaml perl rsync uuid-dev

$ sudo apt-get install kvm

# for dev
$ sudo apt-get install libcurl4-gnuls-dev

#
#
# ISR Server Command
1. Add new user
	$ sudo isr_admin adduser -u test
	$ sudo passwd test

2. generate new VM with ISO file / Can I use current KVM image(qcow2)
1-1) sudo isr_admin import -u test1 -p ubuntuLTS -m 2048 -s 8G -M kvm -F ubuntu.img
1-2) sudo isr_admin makeparcel -u test1 -p ubuntu -m 2048 -s 8G -M kvm


#
#
# ISR Client Command
3. Run ISR at Client side
1) login (requires ssh-agent bash)
	$ isr auth -s dagama.isr.cs.cmu.edu

2) remove all cache
	$ isr lshoard -l -s dagama.isr.cs.cmu.edu -u test1
	$ isr rmhoard 6438c20c-e9ba-4426-88aa-43daf2f93e60 -s dagama.isr.cs.cmu.edu -u test1

3) Resume VM
	$ isr resume parcel -C painful -m kvm -p ubuntu -D -F
	$ isr resume ubuntuLTS -s dagama.isr.cs.cmu.edu -u test1 -C painful -D -F


4. Stop VM
	$ Right Ctrl + Q or close window
1-1) No state save (We do this)
	$ isr clean ubuntuLTS -s dagama.isr.cs.cmu.edu -u cloudlet 
1-2) save back
	$ isr checkin ubuntuLTS -s dagama.isr.cs.cmu.edu -u cloudlet -C painful
	
