Prototype for Cloudlet

# Source dependency
1) C Server
	glib-2.0
	libjson0-dev (JSON manipulation library)
2) Python Script
	pylmza
		compression
		$ sudo easy-install pylzma
3) Android-client

# GIT Basic Command
$ git add README
$ git commit -m 'first commit'
$ git push origin master
