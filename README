Prototype for Cloudlet

# Source dependency
1) C Server
	glib-2.0
	libjson0-dev (JSON manipulation library)
2) Python Script
	xdelta
		binary diff
		$ sudo apt-get install python-xdelta3
	pylmza
		compression
		$ sudo apt-get install python-setuptools python-dev build-essential
		$ sudo easy_install pylzma
	gvncviewer
		vncview for VM interaction, gvncviewer can accommodate resolution changes
		$ sudo apt-get install gvncviewer

3) Android-client

# GIT Basic Command
$ git add README
$ git commit -m 'first commit'
$ git push origin master
