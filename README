Prototype for Cloudlet

# Source dependency
1) C Server
	glib-2.0
	libjson0-dev (JSON manipulation library)

2) Python Script
	xdelta
		binary diff
		$ sudo apt-get install python-xdelta3
	pylmza
		compression
		$ sudo apt-get install python-setuptools python-dev build-essential
		$ sudo easy_install pylzma
	flask
		flask is python web server and cloudlet_cloud.py and isr_run.py has dependency on it
		$ sudo easy_install flask
	gtkvncview
		$ sudo apt-get install gtkvncviewer
	poster
		Only used at emulated_client.py for Multipart HTTP POST
		$ sudo easy_install poster
	


3) Android-client
	all java lib depedency is on Android-client/lib folder, so do not need additional install

	UPnP Discovery
	cling-core-1.0.5, teleal-command-1.0.13

	HTTP Client, 
		apache-mime4j-core, httpmime-4.1.1 - will be erased after ISR test
	

